
#include <bits/stdc++.h>

using namespace std;

int main() {

    // freopen("..\\uva\\p1124.in", "r", stdin);

    /*ios_base::sync_with_stdio(0);
      cin.tie(0);*/

    ios::sync_with_stdio(false);

    char cadena;
    while (scanf("%c", &cadena) != EOF) {
        printf("%c", cadena);
    }

    return 0;
}
